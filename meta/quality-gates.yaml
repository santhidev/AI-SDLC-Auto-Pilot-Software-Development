# Quality Gates — Omni Global

bootstrap:
  - check: mode_no_ask
    rule: "meta/sdlc-state.json.mode must be no-ask before build"

profiles:
  - check: profiles_exist
    rule: "meta/project-profile.yaml, meta/engineering-profile.yaml, meta/design-profile.yaml exist"
