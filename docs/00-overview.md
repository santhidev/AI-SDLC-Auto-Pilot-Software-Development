# Overview / ภาพรวม

## EN
This repo is an AI SDLC AutoPilot. Files are the only source of truth.
Start from latest state:
1) Read `meta/sdlc-state.json`
2) Read profiles `meta/*-profile.yaml`
3) Read `meta/quality-gates.yaml` and `meta/artifact-map.yaml`
4) Run prompts by phase

## TH
Repo นี้คือ AI SDLC AutoPilot ที่ใช้ไฟล์เป็นแหล่งจริงเดียว
เริ่มต่อจากสถานะล่าสุด:
1) อ่าน `meta/sdlc-state.json`
2) อ่าน profiles `meta/*-profile.yaml`
3) อ่าน `meta/quality-gates.yaml` และ `meta/artifact-map.yaml`
4) รัน prompts ตาม phase

Build: 2026-04-25 09:42:29
